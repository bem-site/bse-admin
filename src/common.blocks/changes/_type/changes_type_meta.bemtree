block('changes').mod('type', 'meta')(
    content()(function() {
        return [
            {
                elem: 'title',
                content: 'Meta information'
            },
            applyNext()
        ];
    })
);
