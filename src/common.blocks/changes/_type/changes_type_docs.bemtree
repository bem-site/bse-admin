block('changes').mod('type', 'docs')(
    content()(function() {
        return [
            {
                elem: 'title',
                content: 'Documentation'
            },
            applyNext()
        ];
    })
);
